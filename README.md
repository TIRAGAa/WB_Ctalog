# WB_Сtalog - Парсер 
Что делает парсер и какие функции есть:
1) Собирает данные о товаре и записывает их в файл xlsx.
2) Переменная `sample_query` делает поиск по сайту, и при изменении его можно будет сохранить другие данные.
3) Есть конвертор кириллицы в UTF-8 для url-формата.
^_^